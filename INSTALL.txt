ShopGraph - Full OCR Candidate Pipeline Update

NEW UTILS ORDER

1. Reliable Receipt Detection / Crop
2. Perspective Correction
3. 2x Enlargement
4. Generate OCR Image Variants
5. Run OCR - PSM 4 / 6 / 11
6. Compare OCR Results / Build Raw OCR JSON
7. Export Clean Codebase
0. Return to Main

PIPELINE

Original receipt
    ↓
Reliable crop
    ↓
Perspective correction
    ↓
2x enlargement
    ↓
Grayscale enhanced + thresholded variant
    ↓
Tesseract PSM 4 / 6 / 11 on both variants
    ↓
6 OCR candidates
    ↓
Heuristic comparison
    ↓
Final raw OCR JSON

SIMPLIFIED WORKFLOW

You can run any later stage directly.

For example, if you select option 6 first:
- choose the receipt once
- crop runs automatically
- perspective correction runs automatically
- enlargement runs automatically
- image variants are generated automatically
- all six OCR candidates run automatically
- comparison runs automatically
- final raw OCR JSON is created

OUTPUTS

data/cropped/
    <receipt>_cropped.png

data/perspective_corrected/
    <receipt>_perspective.png

data/enlarged/
    <receipt>_2x.png

data/ocr_variants/
    <receipt>_grayscale.png
    <receipt>_threshold.png

data/ocr_candidates/
    <receipt>_ocr_candidates.json

data/raw_ocr/
    <receipt>_raw_ocr.json

IMPORTANT CLEANUP

Delete the old:
    utils/main.py

Keep:
    utils/utils_main.py

COPY/REPLACE

- main.py
- utils/constants.py
- utils/session_state.py
- utils/image_enlargement.py
- utils/ocr_image_variants.py
- utils/run_ocr.py
- utils/compare_ocr.py
- utils/utils_main.py
- utils/codebase_bundler.py
- extractors/tesseract.py

KEEP YOUR CURRENT

- utils/reliable_receipt_crop.py
- utils/perspective_correction.py
- utils/receipt_picker.py
- utils/export_codebase.py
- utils/__init__.py
